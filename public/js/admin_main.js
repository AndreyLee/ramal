// public/js/admin_main.js

document.addEventListener('DOMContentLoaded', function () {
    // General administrative scripts can be placed here.
    // For example, a global confirmation for all delete buttons if desired,
    // or dynamic form enhancements.

    // Example: Confirm all delete actions that are not already confirmed by inline onsubmit
    // This is a more generic way if you don't want inline JS.
    // const deleteForms = document.querySelectorAll('form[action*="delete"]'); // A bit broad
    // deleteForms.forEach(form => {
    //     form.addEventListener('submit', function(event) {
    //         if (!confirm('Tem certeza que deseja excluir este item? Esta ação não pode ser desfeita.')) {
    //             event.preventDefault();
    //         }
    //     });
    // });

    // console.log("Admin JavaScript loaded.");
});
